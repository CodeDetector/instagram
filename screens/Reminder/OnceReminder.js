import React from 'react'
import { StyleSheet, Text, SafeAreaView } from 'react-native'

const OnceReminder = () => {
    return (
        <SafeAreaView>
            <Text>This is Once Reminder</Text>
        </SafeAreaView>
    )
}

export default OnceReminder

const styles = StyleSheet.create({
    container:{
        flex:1,
        alignItems:"center",
        justifyContent:"center"
    }
})

